# Effect and Effect Atom own application logic outside React

Plain TypeScript owns deterministic rules, Effect services own asynchronous
orchestration and lifetimes, and Effect Atom owns reactive composition and
adapters. React renders published state and dispatches commands, trading familiar
hook orchestration for typed failures, scoped cleanup, and testable seams.
