# Financial values remain exact until execution

Token quantities and financial calculations remain exact decimal or integer
values through validation, preview, and execution; formatting is a presentation
concern. This prevents rounded display values and binary floating-point error
from changing eligibility or transaction inputs.
