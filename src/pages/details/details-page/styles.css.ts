import { style } from "@vanilla-extract/css";

export const tab = style({
  cursor: "pointer",
  userSelect: "none",
});

export const rewardsDot = style({
  position: "absolute",
  top: "-.5px",
  right: "-5px",
});
